language: java
jdk:
  - openjdk11

branches:
  only:
    - master

cache:
  directories:
    - "$HOME/.m2/repository"
    - "$HOME/.gradle"

script: "./gradlew clean build"

after_success:
  - ./gradlew jacocoTestReport coveralls

notifications:
  email:
    recipients:
      - workerhub.official@gmail.com