bootJar { enabled = true }
jar { enabled = false }

dependencies {
    api project(':worker-hub-domain')

    api 'org.springframework.boot:spring-boot-starter-web'
    api 'org.springframework:spring-tx'
}